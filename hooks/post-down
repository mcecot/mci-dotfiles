##
# Remove prezto.
#
if [ -d $HOME/.prezto ]; then
  echo "deleting prezto"
  # rm -rf $HOME/.prezto
fi


if [ -d $HOME/.scm_breeze ]; then
  echo "deleting scm-breeze"
  rm -rf $HOME/.scm_breeze
fi
